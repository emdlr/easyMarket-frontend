import React from "react";
import "./ProductSelector.css";

const ProductSelector = (props) => {
  console.log("ProductSelector props: ", props.productsByStore);
  return <h1>Product Selector</h1>;
};

export default ProductSelector;
